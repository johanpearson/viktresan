import { EmptyState, Page } from '../components/Page.tsx';

export function Logga() {
  return (
    <Page title="Logga">
      <EmptyState>Här kommer du att kunna logga dagens vikt.</EmptyState>
    </Page>
  );
}
